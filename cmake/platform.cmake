# directory
set(PLATFORM_DIR "${TOP_DIR}/platform")
set(PLATFORM_INC_DIR "${PLATFORM_DIR}/include")
set(PLATFORM_SRC_DIR "${PLATFORM_DIR}/src")

# collect bridge sources
set(PLATFORM_SRCS "${PLATFORM_SRC_DIR}/lpfmlib.c")
